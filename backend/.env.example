PORT=3001
# Must be at least 32 characters (see backend/src/index.ts)
JWT_SECRET=erp-upstat-dev-change-me-in-production-32chars
NODE_ENV=development
# PostgreSQL (required). Docker Compose overrides host/port for the db service.
DATABASE_URL=postgresql://erp:erpupstat_change_me@localhost:5432/erpupstat