# Local dev: call API directly.
VITE_API_URL=http://localhost:3001/api

# Production / `npm run build`: see `.env.production` (defaults to /api for same-origin + proxy).
# Optional: when running `npm run serve-demo`, override backend for the preview proxy:
# VITE_PREVIEW_API_TARGET=http://127.0.0.1:3001
