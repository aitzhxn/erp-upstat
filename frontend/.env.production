# Used by `vite build` (and `npm run serve-demo`).
# Relative URL: browser calls same origin as the SPA; Vite preview / nginx proxy forwards /api to the backend.
# Override locally with `.env.production.local` (gitignored), e.g. full Ngrok URL to the API if you do not use a proxy.
VITE_API_URL=/api
